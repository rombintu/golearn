package client

func (c *Client) DeclarationAction(action, title, userID string) (string, error) {
	// var uri string

	// switch action {
	// case "create":
	// 	uri := fmt.Sprintf("http://%s:%s/paper/declaration/create", c.Config.Default.Host, c.Config.Default.Port)

	// }

	// resp, err := http.Post(uri, "application/json", bytes.NewBuffer(json_data))
	// if err != nil {
	// 	return "", err
	// }
	// var res map[string]string

	// if err := json.NewDecoder(resp.Body).Decode(&res); err != nil {
	// 	return "", err
	// }
	return "", nil
}
